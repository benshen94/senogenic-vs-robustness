# Saved Results Index

This folder records generated artifacts for the threshold/noise aging project.

Use `outputs.csv` for durable entries. The expected columns are:

- `date`
- `task`
- `artifact_type`
- `path`
- `source_script`
- `input_paths`
- `description`
- `notes`

For future work, save simulation caches before plotting from them. Use `saved_results/cache/simulations/` for simulation results, `saved_results/cache/plots/` for plot caches, and `saved_results/csv/` for generated tables.

