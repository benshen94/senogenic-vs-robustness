# Legacy Notebooks

These notebooks were migrated from the project root without deleting or rewriting their history.

Many cells still use paths relative to the project root. Open notebooks with the project root as the working directory when possible, or update individual paths before rerunning old cells.

